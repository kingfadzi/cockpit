# =============================================================================
# Governance Cockpit Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------

# Main API base URL (leave empty for relative URLs with Vite proxy)
# VITE_API_BASE=
# VITE_API_BASE=http://localhost:8080
# VITE_API_BASE=https://api.example.com

# Audit API base URL (leave empty for relative URLs with Vite proxy)
# VITE_AUDIT_API_BASE=
# VITE_AUDIT_API_BASE=http://localhost:8081
# VITE_AUDIT_API_BASE=https://audit-api.example.com

# -----------------------------------------------------------------------------
# Development Server Proxy Targets (for npm run dev)
# -----------------------------------------------------------------------------

# Where Vite dev server should proxy /api/* requests
VITE_API_TARGET=http://localhost:8080

# Where Vite dev server should proxy /audit/* requests  
VITE_AUDIT_API_TARGET=http://localhost:8081

# -----------------------------------------------------------------------------
# Mock Data Configuration
# -----------------------------------------------------------------------------

# Set to 1 to use built-in mock data instead of calling backend APIs
# Set to 0 to call real backend APIs
VITE_USE_MOCK=0

# -----------------------------------------------------------------------------
# Example Production Configuration
# -----------------------------------------------------------------------------

# For production deployment, you might use:
# VITE_API_BASE=https://governance-api.company.com
# VITE_AUDIT_API_BASE=https://audit-api.company.com
# VITE_USE_MOCK=0

# For development with different backend:
# VITE_API_TARGET=http://dev-backend.company.com:8080
# VITE_AUDIT_API_TARGET=http://dev-audit.company.com:8081