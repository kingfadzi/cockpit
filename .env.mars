# =============================================================================
# Development Environment Configuration for mars.butterflycluster.com
# =============================================================================

# Backend API base URL for main API
BACKEND_BASE_URL=http://mars.butterflycluster.com:8080

# Audit API base URL  
AUDIT_API_BASE_URL=http://mars.butterflycluster.com:8081

# For Docker Compose environment variables
VITE_API_TARGET=http://mars.butterflycluster.com:8080
VITE_AUDIT_API_TARGET=http://mars.butterflycluster.com:8081

# Set to 0 to use real backend APIs instead of mock data
VITE_USE_MOCK=0