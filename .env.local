# API endpoints for client requests (local development)
VITE_API_BASE=http://localhost:8080
VITE_AUDIT_API_BASE=http://localhost:8081

# API targets for Vite dev server proxy (local development)
VITE_API_TARGET=http://localhost:8080
VITE_AUDIT_API_TARGET=http://localhost:8081

# Set to 1 to use built-in mock data instead of calling backend
VITE_USE_MOCK=0
